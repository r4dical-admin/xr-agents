{
  "name": "Spatial Agent IDE — Electron",
  "build": { "dockerfile": "../Containerfile", "context": ".." },
  "remoteUser": "root",
  "customizations": { "vscode": { "extensions": ["dbaeumer.vscode-eslint"] } }
}
